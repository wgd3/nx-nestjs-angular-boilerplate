import { UpsertDialogDirective } from './upsert-dialog.directive';

describe('UpsertDialogDirective', () => {
  it('should create an instance', () => {
    const directive = new UpsertDialogDirective();
    expect(directive).toBeTruthy();
  });
});
